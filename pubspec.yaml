name: maitri_app
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none
assets:
  - .env

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  bloc: ^8.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  flutter_gen: ^5.8.0
  token_storage:
    path: packages/authentication_client_repo/token_storage
  authenctication_client:
    path: packages/authentication_client_repo/authenctication_client
  supabase_auth_client:
    path: packages/authentication_client_repo/supabase_auth_client
  user_repository:
    path: packages/user_repository
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  form_fields:
    path: packages/form_fields
  api_repository:
    path: packages/api_repository
  shared:
    path: packages/shared
  app_ui:
    path: packages/app_ui
  powersync_repository:
    path: packages/powersync_repository
  l10m: ^0.4.3
  path_provider: ^2.1.5
  hydrated_bloc: ^9.1.5
  envied: ^1.0.0
  flutter_dotenv: ^5.2.1
  dotenv: ^4.2.0
  supabase_flutter: ^2.8.2
  powersync: ^1.10.0
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  google_sign_in: ^6.2.2
  get_it: ^8.0.3
  supabase_auth_ui: ^0.5.4
  flex_color_scheme: ^7.3.1
  equatable: ^2.0.7

dev_dependencies:
  bloc_test: ^9.1.1
  build_runner: ^2.4.14
  envied_generator: ^1.0.0
  flutter_gen_runner: ^5.8.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
